spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/aegis_db
    username: postgres
    password: change_me
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true